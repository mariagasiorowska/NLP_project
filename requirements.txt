# Core (CPU or GPU). Install PyTorch first if you need a specific CUDA build:
#   https://pytorch.org/get-started/locally/
torch>=2.1.0
transformers>=4.40.0
datasets>=2.14.0
accelerate>=0.26.0
safetensors>=0.4.0

# Metrics & evaluation
numpy>=1.22.0
seqeval>=1.2.2

# Llama fine-tuning (LoRA)
peft>=0.10.0

# Figures in generate_figures.py / generate_pertype_figures.py
matplotlib>=3.7.0

# Context-shift split (split_data.py): after pip install, run
#   python -m spacy download en_core_web_sm
spacy>=3.7.0
